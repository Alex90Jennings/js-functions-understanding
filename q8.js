function myFunction(num1, num2) {
  return num1 + num2
}

const num1 = 10
const num2 = 1
const num3 = 4

const result = myFunction(num3, num1)
console.log(result)

//myFunction has 2 arguments, num1 and num2, and returns num1 + num2
//3 variables are declared and initialised num1, num2, num3 with values 10, 1, 4
//function is called with num3 and num1 as inputs
//4+10=14 expected output

//as expected