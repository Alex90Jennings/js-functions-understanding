function myFunction(num1) {
  return -100

  return num1 * 10
}

const result = myFunction(5)
console.log(result)

//myFunction has one argument, num1, and returns -100
//another return of num1 * 10 but will not be reached
//a variable result is declared and initialised with myFunction(5)
//expected result -100

//as expected
